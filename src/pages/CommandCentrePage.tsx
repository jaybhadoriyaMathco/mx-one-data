import { Box, Typography } from "@mui/material";

export function CommandCentrePage() {
  return (
    <Box sx={{ maxWidth: 1100, mx: "auto" }}>
      <Typography variant="h4" sx={{ color: "text.primary" }}>Command Centre</Typography>
    </Box>
  );
}
