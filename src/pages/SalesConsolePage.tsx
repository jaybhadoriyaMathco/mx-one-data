import { Box, Typography } from "@mui/material";

export function SalesConsolePage() {
  return (
    <Box sx={{ maxWidth: 1100, mx: "auto" }}>
      <Typography variant="h4" sx={{ color: "text.primary" }}>Sales Console</Typography>
    </Box>
  );
}
