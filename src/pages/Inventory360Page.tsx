import { Box, Typography } from "@mui/material";

export function Inventory360Page() {
  return (
    <Box sx={{ maxWidth: 1100, mx: "auto" }}>
      <Typography variant="h4" sx={{ color: "text.primary" }}>Inventory 360</Typography>
    </Box>
  );
}
